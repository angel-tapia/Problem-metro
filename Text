Text

# Descripción
Hemos estado esperando ya mucho tiempo la línea $3$ del metro, esto es cansado y agobiante, sin embargo, se ha iniciado ahora el nuevo proyecto del futuro, en la cual tendrás que hacer junto a otros arquitectos, la $4°$ linea, tu tarea es agregar una estacion en la posicion $a$, o saber cual es la estacion mas cercana a la posicion $a$.

Creemos que este es un trabajo muy importante, y tú como programador (y arquitecto) puedes completar esta tarea.

El operador te da  Q acciones, que contienen $t,a$, donde $t$ es el tipo de acción y $a$ es una estación (la $t$ solo puede tener un valor entre $0$ y $1$).

Si $t=1$

Se pregunta entre las estaciones que conoces, el número mas cercano a $a$, si hay empate entre las estaciones cercanas se elije la estación de menor numero, y se regresa ese número.

Si $t=0$

Se agrega la estación en la posición $a$ entre las estaciones que tienes actualmente

# Entrada

La entrada consistirá de un entero $Q$ y $Q$ líneas, que consisten de $t,a$, donde $t$ es el $tipo$ de acción  que se hará y $a$ será la estación que se busca.

# Salida
Las respuestas de la acción tipo $1$, serán separadas por un salto de línea.
# Ejemplo

||input
5
0 5
0 3
1 5
0 10
1 2
||output
5
3
||description
Trivial
||input
6
0 1
1 5
0 10
1 3
0 5
1 9
||output
1
1
10
||end

# Límites

* Para los $100$ pts  $0\leq a_i \leq10^{18}$
y $1\leq Q \leq 10^6$
* Para $90$ pts $0 \leq a_i \leq 10^9$  y $ 
    1\leq Q \leq 10^6$
* Para $60$ pts $0 \leq a_i \leq 10^6$
 y  $1\leq Q\leq 10^5$
* Para $30$ pts $0\leq a_i,Q\leq 1000$
# Agregados culturales

* El joven Arturo se robó el gafete de Tapia
* $Tapia$, el hermano de $Tapia$ es muy agradable
